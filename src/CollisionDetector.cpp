#include "CollisionDetector.h"
#include "Drone.h"

bool CollisionDetector::checkCollision(const std::vector<std::shared_ptr<Drone>> &drones) {
    // simple placeholder: always no collision
    return false;
}
